export enum EResponseStatus {
  Failure = 'failure',
  Success = 'success',
}

export enum ERole {
  Admin = 'admin',
  Guest = 'guest',
  Moderator = 'moderator',
  SuperAdmin = 'superAdmin',
  User = 'user',
}
